<main class="container" style="padding: 80px 24px; text-align: center; max-width: 720px; margin: 0 auto;">
  <div class="eyebrow">Theme port · Session 1</div>
  <h1 class="display-l" style="margin-top: 18px;">Santai theme is <span class="display-italic">live</span>.</h1>
  <p style="margin-top: 24px; color: var(--fg-muted); font-size: 16px; line-height: 1.6;">
    Scaffolding successful. The blank Santai theme renders on <strong>{{ shop.name }}</strong>
    with the Direction C palette (white background, warm-nude accent) and the right type stack
    (Newsreader display + Manrope body).
  </p>
  <p style="margin-top: 16px; font-size: 13px; color: var(--fg-muted);">
    Next session: extract the header + footer + cart drawer from the HTML prototype into
    <code>sections/header.liquid</code> + <code>sections/footer.liquid</code> + <code>snippets/cart-drawer.liquid</code>.
  </p>
  <p style="margin-top: 40px; font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase; color: var(--fg-muted);">
    Theme version 0.1.0
  </p>
</main>
